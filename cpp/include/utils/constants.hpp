#pragma once

// Price increment per integer tick, in currency units.
constexpr double DEFAULT_TICK_SIZE = 0.01;

// Duration of one timestamp unit, in seconds.
constexpr double DEFAULT_TIME_INTERVAL = 1.0;

// Minimum tradeable quantity unit.
constexpr double DEFAULT_LOT_SIZE = 1.0;
